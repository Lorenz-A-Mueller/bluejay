{
  "name": "bluejay",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @bluejay/api run start",
    "build": "yarn workspace @bluejay/api run heroku-postbuild && yarn workspace @bluejay/helpdesk run build"
  }
}
